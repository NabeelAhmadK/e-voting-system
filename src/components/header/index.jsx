import React from 'react';
import { Layout } from 'antd';
const { Header } = Layout
const Headers = () => {
    return (
        <Header className="header-bar">
            <h5>Online Voting</h5>
        </Header>
    )
}

export default Headers;